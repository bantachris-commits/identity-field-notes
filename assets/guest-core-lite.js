(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const isGuest=a=>a?.contentType==='guest'||a?.humanWritten===true;let p;const articles=()=>p||(p=fetch('data/articles.json',{cache:'no-store'}).then(r=>r.json()));function nav(){document.querySelectorAll('.nav-menu').forEach(m=>{if(m.querySelector('a[href="guest-voices.html"]'))return;const a=document.createElement('a');a.href='guest-voices.html';a.textContent='Guest Voices';m.insertBefore(a,m.querySelector('a[href="sponsor.html"]')||null)})}function card(a){const x=a.author||{};return `<article class="card guest-card"><div class="eyebrow">HUMAN-WRITTEN</div><h3><a href="article.html?id=${encodeURIComponent(a.id)}">${esc(a.title)}</a></h3><div class="author-line"><strong>${esc(x.name||'Guest contributor')}</strong>${x.role?`<span>${esc(x.role)}</span>`:''}</div><p>${esc(a.dek||'')}</p><a class="readmore" href="article.html?id=${encodeURIComponent(a.id)}">Read guest voice →</a></article>`}async function page(){const h=document.querySelector('#guestVoicesList');if(!h)return;const d=(await articles()).filter(isGuest).sort((a,b)=>b.date.localeCompare(a.date));h.innerHTML=d.length?d.map(card).join(''):'<div class="empty"><strong>Guest Voices is open.</strong><br><br>Real practitioners only. No invented contributors.</div>'}async function home(){if(!document.querySelector('#archivePreview')||document.querySelector('#guestPreview'))return;const d=(await articles()).filter(isGuest).sort((a,b)=>b.date.localeCompare(a.date));const s=document.createElement('section');s.className='shell';s.id='guestPreview';s.innerHTML=`<div class="section-head"><div><h2>Guest Voices</h2><p>Real humans. Real field experience.</p></div><a href="guest-voices.html">All guest voices →</a></div><div class="cards">${d.length?d.slice(0,3).map(card).join(''):'<div class="card guest-card"><div class="eyebrow">HUMANS WANTED</div><h3>Not every useful thought needs a model.</h3><p>Guest Voices is open to identity practitioners with something useful to say.</p><a class="readmore" href="guest-voices.html">How it works →</a></div>'}</div>`;document.querySelector('main')?.appendChild(s)}async function article(){if(!location.pathname.endsWith('article.html'))return;const id=new URLSearchParams(location.search).get('id'),a=(await articles()).find(x=>x.id===id&&isGuest(x));if(!a)return;let n=0;const go=()=>{const h=document.querySelector('#hero');if(!h?.querySelector('h1')){if(n++<40)setTimeout(go,100);return}h.querySelector('.transparency-box')?.remove();const m=h.querySelector('.meta');if(m){const spans=m.querySelectorAll('span');if(spans[2])spans[2].textContent='HUMAN-WRITTEN'}document.querySelectorAll('.story-tools .pill.ai').forEach(x=>{x.className='pill human';x.textContent='Human contribution'})};go()}function boot(){nav();page();home();article()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();